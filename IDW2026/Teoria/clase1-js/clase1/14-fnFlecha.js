// 0 parámetros: Paréntesis obligatorios
const saludar = () => "¡Hola mundo!";
// 1 parámetro: Paréntesis opcionales
const incrementar = e => e + 1;
// 2 parámetros: Paréntesis obligatorios
const sumar = (a, b) => a + b;



/*// 1. Sintaxis tradicional vs Funciones Flecha
const sumar = (a, b) => {
  return a + b;
};
console.log(sumar(5, 3)); 

// 2. Retorno implícito (en una sola línea)
const multiplicar = (a, b) => a * b;
console.log(multiplicar(4, 2)); 

// 3. Con un solo parámetro (paréntesis opcionales)
const duplicar = numero => numero * 2;
console.log(duplicar(10)); // Imprime: 20*/